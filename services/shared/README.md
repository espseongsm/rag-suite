# Shared Service Infrastructure

Common utilities for the remaining platform services.

## Port Registry

```python
SERVICE_PORTS = {
    "models": 50053,
    "data": 50054,
}
```

The Gateway uses port `50051`.
